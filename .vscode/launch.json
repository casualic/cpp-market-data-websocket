{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug main (lldb)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/bin/market_data_streamer",
      "args": [],
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.env",
      "preLaunchTask": "build"
    }
  ]
}
